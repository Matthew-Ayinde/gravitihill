import { domAnimation } from "framer-motion";

/**
 * Dynamically imported by MotionRoot's LazyMotion so the DOM animation feature
 * bundle never lands in first-load JS. Default export is required by the
 * `features={() => import(...)}` contract.
 */
export default domAnimation;
